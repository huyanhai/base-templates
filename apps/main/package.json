{
  "name": "@micro-app/main",
  "main": "index.ts",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "clean:dist": "rm -rf dist"
  }
}

